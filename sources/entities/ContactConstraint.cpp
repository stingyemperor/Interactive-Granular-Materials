#include "ContactConstraint.hpp"
#include <array>

void ContactConstraint::generateContacts(std::array<std::vector<unsigned int>, max_count> neighbors){
  // find neigbors for each partilce and store in neigbors array
  

  for(int i = 0 ; i < max_count; ++i){

  }
}
